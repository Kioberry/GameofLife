#include <stdio.h>

int load_statefile(FILE* file){
    if(file == NULL){
        return -1;
    }
    return 0;
}

int load_retfile(FILE* file){
    return 0;
}
int store_retfile(FILE* file){
    if(file == NULL){
        return -1;
    }
    return 0;
}